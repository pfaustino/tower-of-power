{
  "name": "tower-of-power",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "3D tower defense — defend crystal outposts from UFO invaders",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm run build",
    "assets:sync": "node scripts/sync-assets.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
